apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: details
  namespace: default 
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: istio/examples-bookinfo-details-v1:1.10.1
            ports: 
            - containerPort: 9080
              name: http1
